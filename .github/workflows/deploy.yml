name: Deploy to Server

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build topi-web
        uses: docker/build-push-action@v6
        with:
          context: ./topi
          push: false
          load: true
          tags: topi-web:latest
          build-args: |
            VITE_API_URL=${{ secrets.VITE_API_URL }}

      - name: Build topi-api
        uses: docker/build-push-action@v6
        with:
          context: ./topi-api
          push: false
          load: true
          tags: topi-api:latest

      - name: Save images to tar
        run: |
          docker save topi-web:latest -o topi-web.tar
          docker save topi-api:latest -o topi-api.tar
          gzip topi-web.tar topi-api.tar
          ls -la *.tar.gz

      - name: Copy files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          source: "docker-compose.yml,topi-web.tar.gz,topi-api.tar.gz"
          target: ${{ secrets.DEPLOY_PATH }}

      - name: Load images and deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            set -e
            cd ${{ secrets.DEPLOY_PATH }}
            echo "Loading images..."
            gunzip -f topi-web.tar.gz topi-api.tar.gz
            docker load -i topi-web.tar
            docker load -i topi-api.tar
            rm -f topi-web.tar topi-api.tar
            echo "Starting containers..."
            docker compose up -d
            docker image prune -f
            echo "Deploy completed"
